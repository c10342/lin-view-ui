import SelectorGroup from "./selector-group.vue";

import "./style.scss";

export default SelectorGroup;

{
  /* <template>
  <div class="l-selector-group" ref="selector" :style="{width:`${width}px`}">
    <label class="l-selector-label" :style="{width:labelWidth}">标签</label>
    <div class="l-selector-list" ref="selectorList" :style="{'height':height}">
      <slot />
    </div>
    <span class="l-selector-more-text" ref="more" v-if="isShowBtn" @click="switchMore">
      <slot name="more">更多</slot>
    </span>
  </div>
</template> */
}
